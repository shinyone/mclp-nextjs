import React from 'react';

const StoreSection = () => {
  return (
    <section id="store-section" className="page-section">
      <p>We are currently not asking players to spend money on our server. We have no store!</p>
    </section>
  );
};

export default StoreSection;